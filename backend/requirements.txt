fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
pydantic==2.7.1
pydantic-settings==2.2.1
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
google-auth==2.35.0
celery==5.4.0
redis==5.0.4
boto3==1.34.100
botocore==1.34.100
numpy==1.26.4
scipy==1.13.0
librosa==0.10.2
tensorflow==2.16.1
scikit-learn==1.4.2
pandas==2.2.2
matplotlib==3.8.4
seaborn==0.13.2
httpx==0.27.0
websockets==12.0
cbor2==5.6.4
aiofiles==23.2.1
pillow==10.3.0
opencv-python-headless==4.9.0.80
tqdm==4.66.4
pytest==8.2.0
pytest-asyncio==0.23.6
transformers==4.41.0
# OWL-ViT (AI labeling, detection mode) is a PyTorch model. Required in prod —
# without torch, _load_owlvit() raises ImportError and detection jobs silently
# produce 0%-confidence unlabeled predictions. CPU wheels; for GPU install from
# the appropriate torch index instead.
torch==2.3.0
torchvision==0.18.0
# Multi-process-safe log rotation (API + Celery workers share channel files).
# Replaces stdlib TimedRotatingFileHandler, which raised WinError 32 on Windows
# during midnight rollover when another process held the file open.
concurrent-log-handler==0.9.29
